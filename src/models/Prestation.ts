export interface PresentationDTO {
    title : string,
    description : string,
    image : string,
}

export interface PresentationToCreateDTO {
    title : string,
    description : string,
    image : string,
}